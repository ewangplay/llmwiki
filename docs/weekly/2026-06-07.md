# 周报：2026-06-01 ~ 2026-06-06

## 高频主题

### 一、脚本底座与工作流系统持续建设

全周贯穿的主题。从周报月报整理（06-01）到课题研究系统发布改造（06-03/06-04），再到敏捷迭代管理系统 `sprint.js` 的设计与交付（06-05），最后讨论多 Agent 架构下脚本底座的共享方式（06-06）。脚本底座从"被使用的工具"逐步演化为"被建设的系统"。

### 二、工具链评估与方案决策

两个分支同时推进：
- **代码补全工具**：GitHub Copilot 请求倍数飙升至 6 倍（06-02），用户主动试用 Claude + DeepSeek 组合并给出正面评价，短期切换完成。
- **图床方案**：围绕旅行足迹系统的图床需求，从 Immich（v2.7.5 升级异常）→ Tailscale 远程访问 → 最终放弃 Immich，落地为本地文件系统 + sharp 的极简方案（06-04）。

### 三、代码仓库治理与记录习惯修正

06-04 修正 MEMORY.md 写入原则，确立"只记录对未来执行决策有实际影响的信息"的纪律。06-05 集中清理 GFA 废弃残留、将提示词和模板统一迁移至 Obsidian 集中目录、拆分 config 命名空间。两条线索都指向——**先清理冗余，再搭建新东西**。

## 重复模式

1. **先清后建**：06-05 花一上午清理废弃层，下午敏捷系统直接站到干净基础上。06-04 修正记忆原则后，后续决策判断更利落。每次新系统搭建前清理旧负担的做法已形成固定节奏。
2. **核查—执行—验证 三步操作顺序**：06-01 代理升级时验证有效，后续系统变更全沿用此顺序。
3. **方案审查前置**：06-05 sprint.js 编码前审查捕获 6 条严重设计问题，全部在开工前修正。06-06 多 Agent 共享方案也是先讨论再落地。模式已从偶发变为自觉。
4. **骑行通勤激发灵感**：06-03 晚上骑车路上连续冒出三个新想法，灵感管理"记录到日记→创建待办→决定是否开课题"三段流程已成型。
5. **定期整理记录**：周报、月报、日记、MEMORY.md 的整理在全周中保持连续节奏。

## 趋势变化

1. **脚本底座从使用到建设，再到治理**：周初是"用它整理记录"→ 周三开始"迭代它本身"→ 周末讨论"多 Agent 如何不破坏它"。关注点从功能使用上升到架构治理。
2. **图床方案从重到轻的收敛**：从全量图床系统 Immich（Postgres + Redis + ML）→ Tailscale 代理 → 本地文件系统 + sharp。外部依赖一步步剥离，最终结论是"先用量级判断工具是否过剩"。
3. **功能开关从 CLI 参数到 Frontmatter 属性**：课题发布控制从 `--no-publish` 改为 `should_publish`/`publish_title` 写进 define 文件元数据层，更声明式、心智负担更低。
4. **单一 Agent 转向多 Agent 中央调度架构**：从直接调用脚本底座 → 由 main 统一代理执行，子 agent 不管理状态。架构清晰度与竞态安全性同步提升。
5. **记录维护从经验积累到纪律化**：MEMORY.md 从"什么都记"到"只记影响决策的"，AGENTS.md/TOOLS.md 新增系统交付时主动补齐入口信息。

## 下周建议

1. **落地本地文件系统 + sharp 图床代码**：方案已验证可行，接口需简洁、与旅行足迹系统无缝对接，约 50-80 行即可实现。
2. **sprint.js 自驱第一个迭代**：将 skill 化评估、GFA/LLMWiki 重合清理等事项作为功能点纳入管理，让敏捷系统自己驱动自己的演进。
3. **持续观察 GitHub Copilot 请求倍数恢复情况**：确认是否需要长期停用 Copilot、固定使用 Claude + DeepSeek 组合。
4. **配置 Tailscale ACL**：限制手机端仅能访问 Immich 端口，不暴露内网其他设备。
5. **复查已有课题的 `should_publish` 默认值**：避免旧课题意外跳过发布流程。
6. **设计子 agent 只读查询接口**：后续 sage/coder 如需查询课题已有素材，仍走 main 转发路径，避免引入新的竞态风险。
## 今日一句话总结
今天一边把“课题完成→Hexo 发布”这条链路继续跑通，一边把 LLMWiki 的最小工作流重新收紧，开始优先保住稳定骨架，再慢慢补主题和展示层。

## 今日主题
- 课题系统到 Hexo Blog 的发布联动
- Hexo 发布链路细节修正
- LLMWiki 最小工作流收缩
- 主题功能暂时禁用与脚本联动
- 先把日报/周报/月报/画像跑稳，再扩展主题

## 今日洞察
1. **课题系统到博客发布的闭环已经基本成立。**  
   今天测试后确认，完成课题时同步把定稿文章发布到 Hexo Blog 这条链路已经跑通，说明课题系统不只是内部收口，而是能直接进入博客输出。

2. **发布链路真正要稳，细节比想象中多。**  
   目前还剩几个小问题：`define` 文件里的 `tags` 和 `categories` 没有同步过去，且同步后会被清空。更关键的是，这两个属性在源文件和目标文件里的格式不同，必须做格式转换，不能直接硬拷贝。

3. **Hexo 发布不仅是生成文章，还包括最后的 git 提交。**  
   你已经明确把“同步到 Hexo Blog 后要提交 git”作为最后一步，并希望复用现有的 `git-utils` 脚本来做。这说明你看的不是单次生成，而是完整发布收口。

4. **LLMWiki 现在的优先级是“先跑最小工作流”，不是急着扩功能。**  
   你已经把主题生成和展示先禁用，保留日报、周报、月报、个人画像这条主链路。这个选择很明确：先把最小闭环优化到位，再谈主题扩展。

5. **主题功能不是不要了，而是先暂时收起来。**  
   你强调“禁用而不是删除”，说明你对系统演进的态度是保守可回退的：先用开关关掉，未来需要时还能重新打开。

6. **最关键的不是链路多，而是提示词质量。**  
   你已经明确说了，在 LLMWiki 质量还没有优化到位之前，先把生成提示词调好，再扩展主题。这说明你在意的是产出质量，而不是一味把流程做复杂。

## 今日建议
- 继续优先修 Hexo 同步中的 `tags/categories` 格式转换问题，别让源文件和目标文件的格式差异拖住发布链路。
- 把“同步到 Hexo 后再 git 提交”做成固定收尾动作，尽量复用现有 `git-utils`。
- LLMWiki 先保持最小工作流：日记 + 工作日志 → 日报 → 周报 → 月报 → 个人画像。
- 主题功能继续以“禁用开关”方式保留，等最小工作流稳定后再恢复。
- 把提示词优化当作当前最优先的质量工作，先把最小链路的输出质量稳住，再谈扩展。